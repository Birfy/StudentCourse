package com.example.studentcourse.repository;

import com.example.studentcourse.model.StudentCourseRelationship;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StudentCourseRelationshipRepository extends JpaRepository<StudentCourseRelationship, Long> {
}
